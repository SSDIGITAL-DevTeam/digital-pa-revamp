import { seoMetadata } from '@/constants/metadata/metadata'; 

export const metadata = seoMetadata.aiMarketingAutomation; 

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
